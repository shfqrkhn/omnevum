{
  "name": "omnevum",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "generate:control": "node scripts/generate-control.mjs",
    "audit:structure": "node scripts/structure-audit.mjs",
    "audit:architecture": "node scripts/architecture-audit.mjs",
    "audit:static": "node scripts/static-audit.mjs",
    "audit:references": "node scripts/reference-audit.mjs",
    "audit:acceptance": "node scripts/acceptance-audit.mjs",
    "audit:drift": "npm run generate:control && git diff --exit-code -- docs/control",
    "ci": "npm run generate:control && npm run audit:structure && npm run audit:references && npm run audit:acceptance && npm run audit:architecture && npm run typecheck && npm test && npm run build && npm run audit:static && npm run audit:drift && git diff --check"
  },
  "devDependencies": {
    "@types/node": "26.6.1",
    "fake-indexeddb": "6.2.5",
    "typescript": "7.0.2",
    "vite": "8.3.0",
    "vitest": "5.0.1"
  }
}
