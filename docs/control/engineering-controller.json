{
  "schemaVersion": 1,
  "kind": "engineering-controller",
  "status": "PHASE_0_IN_PROGRESS",
  "scope": "Omnevum finite accepted product completion",
  "baseline": {
    "omni": "docs/Omni_3.32.0.md",
    "mpes": "docs/Omnevum-MPES-v0.12.0-converged.md",
    "phaseGate": "PLATFORM_READY_FOR_CORE"
  },
  "verified": [
    "workspace baseline reconciled",
    "initial static PWA shell implemented",
    "canonical IndexedDB store implemented",
    "Vault export/import validation implemented",
    "initial command and provenance tests implemented",
    "machine-readable requirement and acceptance projections generated",
    "required companion control registers materialized",
    "structure audit, strict typecheck, unit tests, and production build pass",
    "exact Phase 0 upstream characterization recorded",
    "search/schema/analysis/parser reuse bake-off recorded; MiniSearch selected behind the derived-index owner",
    "local browser smoke recorded with reload persistence, capture, search, and theme evidence",
    "MVP nucleus smoke recorded for Space, triage, Time observation, summary, and relationship reference flow",
    "bounded Artifact ingest/Vault payload round-trip implemented",
    "platform-owned Effect/Outbox state store and secret-field guard implemented",
    "Recovery surface exposes archive restore, derived-index repair, and privacy-minimized diagnostics",
    "qualified en-CA/fr-CA presentation locale persists without changing canonical identities",
    "equal-revision semantic Vault conflicts are detected and reported",
    "durable Effect/Outbox runner recovers interrupted operations through explicit reconciliation",
    "bounded quantitative Track/Observe capture and derived Home signals are implemented",
    "multilingual derived Search is selected behind the rebuildable index owner",
    "Space membership/filter UI preserves one canonical record across multiple scopes",
    "capability startup/runtime failures are isolated behind the platform runtime",
    "History UI exposes field-level revision snapshots without creating a second writable copy",
    "Safe Presentation Mode selects a known-good profile without rewriting malformed presentation or canonical records",
    "persisted Compose/View dashboard projects accessible list, table, chart, and capture boundaries",
    "production preview service worker is activated and controls the page; stamped cache identity, offline reload/mutation, 390x844 labelled keyboard focus, and no browser warnings are evidenced",
    "Device/Input broker mediates bounded file and clipboard Acquire staging through the existing preview and canonical command path",
    "local service-worker update/rollback rehearsal precaches the new generation, retains the prior cache, and serves an offline rollback capture",
    "derived-search corruption is detected and rebuilt without changing canonical records",
    "injected storage pressure reclaims only derived state, exposes persistence status, and preserves canonical records",
    "Acquire rejects embedded-credential and oversized URLs and retains hostile-looking text as inert data",
    "privacy-minimized diagnostics expose category-level runtime states without canonical content",
    "current production preview renders hostile Acquire text literally and completes category diagnostics export",
    "Vault merge preview validates integrity and conflict outcomes without mutation",
    "boot recovery exports a bounded read-only raw-state snapshot when malformed canonical state prevents a portable Vault",
    "boot recovery offers an explicit repair-from-snapshot path that retains valid canonical records and marks derived search state for rebuild",
    "stale IndexedDB clients are fenced after a version upgrade rather than continuing unsafe reads/writes",
    "an interrupted IndexedDB upgrade is aborted and a fresh store reopen retains the exact canonical record",
    "ordinary canonical records remain usable through the IndexedDB fallback when optional persistence/OPFS APIs are unavailable",
    "GitHub Pages deployment workflow runs the locked CI/build gate and uploads only the built dist artifact; live deployment remains unverified",
    "current receipt-bound built artifact completes Chromium capture, theme, and reload persistence on a fresh local static origin",
    "115 unit tests pass across 41 test files after the source-meaning workbench increment",
    "current built-artifact Chromium preview completes the integrated MVP workflow, localized rebrand/reload, and confirmed Vault merge",
    "current receipt-bound Chromium preview completes Evidence link creation, quote-validated source annotation, GeoJSON Point Place capture, reload persistence, and zero console warnings/errors",
    "current receipt-bound Chromium preview proves grant-bound private-share omission, explicit private inclusion, declared-Space enforcement, purpose-bound grant creation/revocation, revoked-export disablement, reload persistence, and zero console warnings/errors",
    "current receipt-bound Chromium preview retains mixed English/French source text, searches an English token under fr-CA UI, persists locale/text after reload, and returns zero console warnings/errors",
    "current receipt-bound Chromium single-origin preview pulls one bounded remote sentinel through JsonEndpointTransport, imports it through the canonical store, pushes the merged replica, and returns zero console warnings/errors",
    "current receipt-bound Chromium preview splits an ambiguous triage source into bounded canonical note parts, archives the staging source, clears the inbox, preserves source provenance, and exercises task-part output in the command regression",
    "current receipt-bound Chromium preview defers a triage source to an explicit future due time, hides it until due, then re-surfaces a due item on the same origin without claiming background delivery",
    "current receipt-bound Chromium preview keeps default captures in the inbox and, only when the visible Safe direct route checkbox is selected, creates an unambiguous capture as REVIEWED outside the inbox",
    "triage CommandBus regression records an explicit DELETED disposition before archiving a source for reversible Recovery restore; the built UI exposes the separate Delete action",
    "current triage link/route/split/delete transitions use one revision-fenced IndexedDB batch with atomicity regression coverage; the release receipt is bound to source 310d278",
    "current built-artifact Chromium preview exposes proposal-only triage owner/type/action guidance without canonical mutation; the release receipt is bound to source 0aaa935",
    "bounded automation evaluator emits only proposal-only actions from the explicit admitted semantic-command set and rejects network.fetch/prototype/budget abuse; source bbc902d"
  ],
  "unverified": [
    "NeumanOS/Kurumi launchpad benchmark and final currentness/support qualification beyond the recorded harvest-reference decision",
    "SelfStore/remoteStorage contract, security, quota, migration, provider, and cross-origin qualification beyond the recorded candidate decision",
    "production static-host, browser-version, responsive, and assistive-technology evidence",
    "production static-host service-worker update/rollback and cross-origin deployment proof",
    "full Phase 0 acceptance gate, native quota/canonical-migration matrix, browser recovery interaction, and recovery matrix",
    "broader security/untrusted-content/credential, active-content, egress, and production boundary evidence",
    "static-host deployment, smoke, and rollback proof",
    "richer Time/Track/Compose capability contracts and target qualification",
    "heterogeneous Acquire import, GPX/timeline Place reuse, external Web-document preservation, and spatial-analysis qualification",
    "remote share delivery/effect use, provider ACL interaction, stale-share reconciliation, and cross-target disclosure qualification",
    "mixed-language Vault import/summary and AI/tool language-neutral semantic-ID qualification",
    "provider-backed or consumer-cloud sync, transport authentication/credential-key brokering, offline rejoin UI, and external-effect replay evidence",
    "MVP release and later phases"
  ],
  "nextAction": "Execute the Pages workflow on the configured remote repository, then run the Phase 0 support/accessibility/offline/update and native fault-injection matrix, including failed-open repair and recovery-snapshot handling, before promoting the MVP gate; keep the MiniSearch adapter target qualification open and do not treat workflow configuration or local preview as deployment proof.",
  "completionClaim": "Do not claim 100_PERCENT_COMPLETE until all mandatory requirements and scenarios have evidence-backed PASS or justified NOT_APPLICABLE."
}
